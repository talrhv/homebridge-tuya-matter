"use strict";

/**
 * Keep these in sync with:
 * - package.json "name"
 * - config.schema.json "pluginAlias" / platform name
 */

const PLATFORM_NAME = "TuyaPlatformMatter";
const PLUGIN_NAME = "homebridge-tuya-matter";

export default {
  PLATFORM_NAME,
  PLUGIN_NAME,
};
